import { Textarea } from "./ui/textarea";

const ImageUploadForm = () => {
  return (
    <div>
      <Textarea placeholder="What makes this place interesting?" id="message" />
    </div>
  );
};

export default ImageUploadForm;
